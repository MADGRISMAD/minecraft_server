[package]
name = "minecraft_server"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1", features = ["full"] }
bytes = "1.3.0"
serde = { version = "1.0", features = ["derive"] }
